# Logs
logs
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*
lerna-debug.log*

node_modules
dist
dist-ssr
*.local

# Local env — never commit secrets
.env
.env.local
.env.*.local
# .env.production is intentionally committed (no secrets, just public API URLs)
# so Vercel picks it up during build without needing dashboard env vars

# Editor directories and files
.vscode/*
!.vscode/extensions.json
.idea
.DS_Store
*.suo
*.ntvs*
*.njsproj
*.sln
*.sw?
